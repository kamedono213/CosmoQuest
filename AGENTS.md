# Cosmo Quest 開発ルール

このリポジトリのすべての開発作業で、以下を標準ルールとして適用する。

## 基本方針

- 保守性、可読性、拡張性、データ駆動設計を最優先する。
- 場当たり的な実装を避け、数年間の継続的な更新に耐える設計にする。
- JSONやTypeScriptで管理できる仕様・コンテンツはハードコーディングしない。
- コードレビューの観点で改善点を見つけた場合は、既存機能を壊さない範囲で提案・改善する。
- PWA、Service Worker、オフライン、Topic、問題、図鑑、Journey、ステージ、ランキング、学習ツリーの既存仕様を維持する。

## UI・世界観

- 「勉強アプリ」ではなく、学ぶほど宇宙船、基地、図鑑、惑星、実績などが増える「宇宙探査RPG」として設計する。
- 宇宙・未来・子どもにも分かりやすい・ワクワクする世界観を守る。
- スマートフォンを最優先し、レスポンシブ、軽量、滑らかなアニメーションを実現する。
- 画像表現の優先順位はCSS、SVG、Canvas、画像とする。
- 文章だけでは理解しにくい内容は、CSSまたはSVGを中心に教育用図解として実装する。

## アーキテクチャ

- 宇宙船レベル／コレクション、宇宙基地、宇宙マップ、惑星探査、アーティファクト、図鑑完成率、トロフィー、デイリー／ウィークリー、ログインボーナス、ショップ、コイン、称号、BGM、宇宙服、イベント、研究ツリー、季節イベント、クラウドセーブ、プッシュ通知、ランキングを追加しやすい境界とデータモデルを維持する。
- コンテンツ、ゲーム状態、表示、永続化、外部同期の責務を分離する。
- 保存データはバージョン管理とマイグレーションを前提にし、既存ユーザーデータを失わない。

## 品質ゲート

実装完了時は必ず以下を確認し、エラーが残る状態ではpushしない。

- `git status`
- `next build`とTypeScript（エラー0件）
- ESLint
- PWA manifestとService Worker
- ブラウザConsole Error
- JSONコンテンツ
- 画像欠損
- レスポンシブ／モバイル表示
- 保存データと既存機能の回帰

## Git・リリース

- 大きな機能追加の完了ごとにチェックポイントコミットを作成する。
- 実装内容が分かるConventional Commits形式のメッセージを使用する。
- 品質ゲート通過後にGitHubへpushする。pushできない場合は理由を報告する。
- push後はVercelデプロイを確認し、失敗時は原因を修正する。
- 公開URLでトップ画面、Topic、問題、図鑑、PWA、Service Workerを確認する。

## Vercel運用

- このリポジトリはGitHubとVercelを連携して運用する。
- 初回デプロイ後は、GitHubの`main`ブランチへのpushをVercelの自動デプロイトリガーとする。
- push後はGitHub Actions（存在する場合）とVercel Deploymentの成功を確認する。
- 公開URLがHTTP 200を返すことを確認する。
- 公開環境でPWA manifest、Service Workerの登録と更新、Console Error 0件を確認する。
- デプロイが失敗した場合は原因を修正し、品質ゲートを再実行して、成功するまで修正コミットとpushを繰り返す。
- 初回Vercel連携が未完了の場合は、推測で別プロジェクトを作成せず、未連携であることと必要な設定を報告する。
- 作業完了報告には、毎回Vercel Deployment結果と公開URLを含める。

## 完了報告

作業終了時は次を報告する。

- 実装内容
- 変更ファイル
- コミットハッシュ
- GitHub push結果
- Vercelデプロイ結果
- 公開URL
- ビルド結果
- 残課題
