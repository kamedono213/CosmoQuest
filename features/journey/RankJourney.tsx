"use client";
import { useState } from "react";
import ranksJson from "@/content/ranks.json";
import chaptersJson from "@/content/chapters.json";
import { RubyText as R } from "@/components/RubyText";
import type { UserData } from "@/domain/models";
type Rank={id:string;name:string;stars:number;subtitle:string;internalGrade:number;color:string};type Chapter={id:string;rankId:string;order:number;title:string;stageIds:string[]};
export function RankJourney({user,openStage,openTest}:{user:UserData;openStage:(id:string)=>void;openTest:()=>void}){const[selected,setSelected]=useState("vega");const ranks=ranksJson.items as Rank[];const chapters=(chaptersJson.items as Chapter[]).filter(c=>c.rankId===selected);const rank=ranks.find(r=>r.id===selected)!;return <section className="rank-journey"><header><small>LEARNING JOURNEY</small><R as="h2" text="星(ほし)の航路(こうろ)を選(えら)ぶ"/></header><div className="rank-selector">{ranks.map(r=><button key={r.id} className={selected===r.id?"active":""} style={{"--rank-color":r.color} as React.CSSProperties} onClick={()=>setSelected(r.id)}><b>{"⭐".repeat(r.stars)}</b><strong>{r.name}</strong><R as="small" text={r.subtitle}/></button>)}</div><div className="course-head"><span>{"⭐".repeat(rank.stars)}</span><div><small>SELECTED COURSE</small><strong>{rank.name}</strong></div></div><div className="chapter-path">{chapters.map((c,i)=>{const complete=c.stageIds.length>0&&c.stageIds.every(id=>user.progress.completedStageIds.includes(id));const available=i===0||chapters[i-1].stageIds.every(id=>user.progress.completedStageIds.includes(id));const certification=c.id.endsWith("certification")||!c.stageIds.length;return <button key={c.id} className={complete?"complete":available?"available":"locked"} disabled={!available} onClick={()=>certification?openTest():openStage(c.stageIds[0])}><span>{complete?"✓":available?i+1:"🔒"}</span><R as="strong" text={c.title}/><small>{c.stageIds.length?<>{c.stageIds.filter(id=>user.progress.completedStageIds.includes(id)).length}/{c.stageIds.length}</>:"CHALLENGE"}</small></button>})}</div><R as="p" text="どの航路(こうろ)もベガの進行状況(しんこうじょうきょう)に関係(かんけい)なく挑戦(ちょうせん)できます。"/></section>}
